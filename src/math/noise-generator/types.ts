export enum BernoulliNoiseType {
    DEFAULT,
    ISOLATED,
    ISOLATED_BIG,
}

export enum BiasType {
    LOWER,
    UPPER,
    CENTER,
    BOUNDS,
    BOUNDS_BY_CUBIC,
    BOUNDS_BY_QUINTIC,
    BOUNDS_BY_SEPTIC,
    BOUNDS_BY_TRIG,
}
